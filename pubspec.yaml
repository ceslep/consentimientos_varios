name: consentimientos_varios
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  flutter_drawing_board: ^0.9.3
  get: ^4.6.6
  http: ^1.2.2
  intl: ^0.19.0
  shared_preferences: ^2.3.2
  snack: ^2.0.0
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
